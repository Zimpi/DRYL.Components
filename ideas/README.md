# Ideas

One file per idea, `I{n} {Name}.md`, maintained while the idea is still in
dialogue. Rules: [`../harness/ideas.md`](../harness/ideas.md).

The process is binding: a new feature, a larger change or anything not yet
specified starts here as a dialogue, never as code and never as a spec.
