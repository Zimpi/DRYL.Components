namespace DRYL.Components;

/// <summary>Overall size of a <see cref="DrylEmptyState"/>.</summary>
public enum EmptyStateSize
{
    /// <summary>Compact — for inline / in-panel empty states (e.g. a dropdown).</summary>
    Small,
    /// <summary>Default — for full-page or large-card empty states.</summary>
    Medium
}
