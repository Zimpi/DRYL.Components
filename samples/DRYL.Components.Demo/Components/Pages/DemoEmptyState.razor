@page "/empty-state"

<PageTitle>DRYL — Empty State</PageTitle>

<div class="col fade-in" style="gap: var(--sp-7);">

    <div class="col" style="gap: var(--sp-2);">
        <span class="eyebrow">Feedback</span>
        <h2>DrylEmptyState</h2>
        <p class="lead">
            Centred placeholder for "no data" situations — empty tables, search
            results, chat panels or dashboards. Icon, title, description and an
            optional call-to-action slot.
        </p>
    </div>

    <section class="col" style="gap: var(--sp-3);">
        <h3>Default</h3>
        <div class="glass-card p-default">
            <DrylEmptyState Icon="Search" Title="No results found"
                            Description="We couldn't find anything matching your search. Try a different term or clear the filters.">
                <ActionContent>
                    <DrylButton Variant="DrylButton.ButtonVariant.Secondary" LeadingIcon="X">Clear filters</DrylButton>
                    <DrylButton Variant="DrylButton.ButtonVariant.Primary" LeadingIcon="Plus">New item</DrylButton>
                </ActionContent>
            </DrylEmptyState>
        </div>
    </section>

    <section class="col" style="gap: var(--sp-3);">
        <h3>Small (in-panel)</h3>
        <div class="glass-card p-tight" style="max-width: 320px;">
            <DrylEmptyState Size="EmptyStateSize.Small" Icon="Bell"
                            Title="You're all caught up"
                            Description="No new notifications." />
        </div>
    </section>

    <section class="col" style="gap: var(--sp-3);">
        <h3>Minimal</h3>
        <div class="glass-card p-default">
            <DrylEmptyState Title="Nothing here yet" />
        </div>
    </section>

    <section class="col" style="gap: var(--sp-3);">
        <h3>AI mode</h3>
        <p class="lead">
            The empty state is often exactly the moment the AI is at work —
            generating, analysing or streaming content into a blank area. Pass
            the shared <code>Ai</code> parameter so the placeholder carries the
            same aura vocabulary as the rest of the library.
        </p>
        <div class="row" style="gap: var(--sp-4); flex-wrap: wrap;">
            <div class="glass-card p-default" style="flex: 1; min-width: 280px;">
                <DrylEmptyState Ai="AiState.Thinking" Icon="Sparkle"
                                Title="Analysing your data…"
                                Description="The assistant is reviewing your dataset to surface insights." />
            </div>
            <div class="glass-card p-default" style="flex: 1; min-width: 280px;">
                <DrylEmptyState Ai="AiState.Streaming" Icon="Sparkle"
                                Title="Drafting a summary…"
                                Description="Generating an overview from the documents you uploaded." />
            </div>
            <div class="glass-card p-default" style="flex: 1; min-width: 280px;">
                <DrylEmptyState Ai="AiState.Generated" Icon="Check"
                                Title="Ready"
                                Description="The AI finished — your results will appear here." />
            </div>
        </div>
    </section>

</div>
