namespace DRYL.Components;

/// <summary>Horizontal alignment of a column's cells and header.</summary>
public enum ColumnAlign
{
    /// <summary>Left-aligned (LTR) / right-aligned (RTL). Default.</summary>
    Start,

    /// <summary>Center-aligned.</summary>
    Center,

    /// <summary>Right-aligned (LTR) / left-aligned (RTL). Use for numeric columns.</summary>
    End
}
